{
  "name": "vendor-onboarding-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "test:e2e": "npm run test:e2e --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
